#ifndef CORE_STRING_HPP
#define CORE_STRING_HPP

#include <core/types.hpp>

// Get String Length
extern uint64 string_length(const char *string);

// Compare Strings
extern bool string_compare(const char *a, const char *b);

#endif